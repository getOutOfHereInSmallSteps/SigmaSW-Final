import React from 'react';

import { ReactComponent as Burger } from '../../../svg/burger.svg';

const MenuBurger = props => {
  return <Burger className={props.className} onClick={() => {}} />;
};

export default MenuBurger;
